<h3>Comments:</h3>
<% if @project.comments %>
	<% for comment in @project.comments do %>
	  <ol>
 	   <h4><%= link_to comment.user.name, comment.user %>, said:
 	   <p><%= comment.content %> <%= render 'shared/rem', obj: comment %></p>
	  </ol>
	<% end %>
<% end %>