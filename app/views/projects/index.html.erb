<% provide(:title, "All Projects") %>
<div class="row-fluid">
	<div class="span8">
		<h2> Join the Biggest Creative Collective and Let's Start Working Together!</h2>
	</div>
	<div class="span 3">
		<a class="btn btn-large btn-primary" href="#"> Start Project!</a>
		<a class="btn btn-large" href="#"> Explore Projects!</a>
	</div>
</div>
<!--h1>All projects</h1-->
<%= will_paginate %>

<ul class="thumbnails"> <!-- generar class projects-->
	<%= render @projects %>
</ul>

<%= will_paginate %>