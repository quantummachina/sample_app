<h3>File Upload</h3>
<%= form_for :upload_file, html: {multipart: true} do |g| %>
	<%= g.file_field :user_photo, accept: 'image/png,image/gif,image/jpeg'  %>
	<!-- %= f.submit "Upload" %-->
<% end %>